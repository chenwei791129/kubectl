name: Auto check and create tag when kubectl new version released

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:     
  check-and-tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check new version
        id: newversion
        run: echo "NEW_VERSION=$(curl -s https://api.github.com/repos/kubernetes/kubernetes/releases/latest | jq -r '.tag_name')" >> $GITHUB_OUTPUT
      - name: Print new version
        run: echo new version is ${{ steps.newversion.outputs.NEW_VERSION }}
      - name: Check version not exist
        uses: mukunku/tag-exists-action@v1.1.0
        id: checkexist
        env:
          # Use PAT instead of GITHUB_TOKEN to enable triggering other workflows
          # GITHUB_TOKEN cannot trigger workflows on tag push events for security reasons
          GITHUB_TOKEN: ${{ secrets.PUSH_PAT }}
        with: 
          tag: ${{ steps.newversion.outputs.NEW_VERSION }}
      - uses: actions/checkout@v3
        with:
          # Use PAT to allow tag creation that can trigger build-and-push workflow
          # Default GITHUB_TOKEN prevents triggering workflows from automated tag pushes
          token: ${{ secrets.PUSH_PAT }}
      - name: Add tag when new version released
        if: ${{ steps.checkexist.outputs.exists != 'true' }}
        uses: rickstaa/action-create-tag@v1
        with:
          tag: ${{ steps.newversion.outputs.NEW_VERSION }}
          # Use PAT to ensure the created tag can trigger the build-and-push workflow
          github_token: ${{ secrets.PUSH_PAT }}
      - name: Push tag to remote
        if: ${{ steps.checkexist.outputs.exists != 'true' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch --tags
          git push origin ${{ steps.newversion.outputs.NEW_VERSION }} --tags
